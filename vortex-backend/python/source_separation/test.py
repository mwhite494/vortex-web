import music_to_visual

# Replace `PATH_TO_AUDIO_FILE` with the path to your audio file
audio_file = "PATH_TO_AUDIO_FILE"

# Call the `music_to_visual` function to generate the visualization data
visualization_data = music_to_visual(audio_file)

# Print the visualization data to the console
print(visualization_data)
